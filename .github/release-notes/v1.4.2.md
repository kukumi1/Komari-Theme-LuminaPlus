# Komari-Theme-LuminaPlus v1.4.2

## 修复

- 修复 Ping 图表切换时间范围后线路筛选被清空的问题。此前只保留一条线路查看时，切到「1 小时 / 4 小时 / 1 天」任意档位，隐藏的线路都会重新全部显示。

  根因是切换时间范围会更换查询键，新数据到达前任务列表短暂为空，而清理逻辑把「数据还没加载」误当成「任务已被删除」，连带把用户的筛选状态一起清掉。现在只在任务确实消失时才丢弃对应的隐藏状态，筛选在各时间档位之间保持不变。

  切换节点时仍会重置筛选（不同节点的监测任务不同），横向缩放区间也仍会在切换时间范围时回到完整范围。
